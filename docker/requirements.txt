# Weather data access
herbie-data>=2024.0.0

# Geospatial processing
rioxarray>=0.15.0
xarray>=2023.0.0
cfgrib>=0.9.10

# AWS integration
boto3>=1.28.0

# Configuration
pyyaml>=6.0

# Data processing utilities
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Pin to NumPy 1.x for GDAL compatibility

# HTTP utilities (for Herbie)
requests>=2.31.0

# Additional dependencies for xarray and rioxarray
netCDF4>=1.6.0
h5netcdf>=1.2.0
dask[complete]>=2023.0.0
